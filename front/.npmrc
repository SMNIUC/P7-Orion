# Enforce the toolchain declared in package.json "engines".
# Prevents a stray `npm install` under Node 23 / npm 11 from silently
# rewriting package-lock.json into a form that breaks `npm ci` in CI/Docker.
engine-strict=true
